<h1>Welcome to the Oregon Train Website!</h1>
<h3>A place where you can store and view trains, cities, and stop times.  You can also purchase a ticket!</h3>

<p><a class='btn btn-default' href="/trains">Add a new train</a></p>
<p><a class='btn btn-default' href="/cities">Add a new city</a></p>

<h3>Here are all the trains in this database:</h3>

<% if @trains.any? %>
  <ul>
    <% @trains.each do |train| %>
      <%= "<li><a href='/trains/#{train.id}'>#{train.color}</a></li>" %>
    <% end %>
  </ul>

<% else %>
  <p>No trains have been added yet!</p>
<% end %>


<h3>Here are all the cities in this database:</h3>

<% if @cities.any? %>
  <ul>
    <% @cities.each do |city| %>
      <%= "<li><a href='/cities/#{city.id}'>#{city.name}</a></li>" %>
    <% end %>
  </ul>

<% else %>
  <p>No cities have been added yet!</p>
<% end %>

<h3>Here is a timetable of all train stops:</h3>

<% if @timetable.any? %>
  <table class="table">
    <thead>
      <tr>
        <th>Arrival Time</th>
        <th>Train</th>
        <th>City</th>
      </tr>
    </thead>
    <tbody>
      <% @timetable.each do |stop|%>
        <%="
        <tr>
          <td>#{stop.fetch(:time)}</td>
          <td>#{stop.fetch(:train)}</td>
          <td>#{stop.fetch(:city)}</td>
        </tr>
        "%>
      <% end %>
    </tbody>
  </table>
<% end %>
